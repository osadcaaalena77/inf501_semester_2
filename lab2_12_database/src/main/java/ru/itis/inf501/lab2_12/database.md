# Строим базу данных

1. Для чего? - Храним в удобном (структурированном) виде информацию
2. Что будем хранить? - Данные о доходах и расходах
3. Что потом? - Будем делать отчеты, узнаем сколько тратим в день, неделю, ...
4. В каком формате будем хранить? - В виде таблиц
5. Сколько таблиц будет? - Столько, сколько понадобиться
6. Какой формат хранения? - Бинарный. Можно было бы json или csv, но это текст, требующий конвертации, будут просадки в скорости

## Структура данных
```java
public class Many implements Serializable {
    private Integer id;
    private int type;  // 1 - доход, -1 расход 
    private String name;
    private Date date;
    private String comment;
}
```

## Структура файла данных

1. Бинарный файл с именем `many.tbl`
2. Содержит последовательный набор записей
3. Структура записи:
- id (int)
- flag (byte) - флаг актуальности (1-актуальная, 0-удаленная)
- size (int) - размер сериализованного объекта
- массив байт размера size, содержащий сериализованный объект

### Задачи
1) Записать данные в конец файла
2) Прочитать данные с указанным id
3) Изменить флаг актуальности
4) Удалить
5) Сделать индекс:
```java
class Index {
    int id; // id записи
    long position; // позиция в файле базы данных, начиная с которой идут данные этой записи
    // переопределить equals и hash для структур быстрого поиска по id
}

Set<Index> index; // список всех данных о записях в БД. Подобрать список с логарифмическим поиском по id
При добавлении записи - добавлять в index, при удалении записи - удалять из index соответствующую информацию, при 
изменении данных - изменять position в индексе
```
Индексную структуру сохранять в файл `student.idx` при завершении программы, считывать из файла при старте программы

6) Реализовать консольный интерфейс для операций:
- добавление в БД
- поиск по ID
- удаление по ID
- изменение данных
- вывод всех данных в файл `students.html` следующего вида:
```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
</head>
<body>
<h1>Доходы/Расходы</h1>
<table>
    <tr>
        <th>id</th><th>Тип</th><th>Название</th><th>Сумма</th><th>Дата</th><th>Примечание</th>
    </tr>
    <tr>
        <td>1</td><td>Доход</td><td>Стипендия</td><td>10000</td><td>03.04.2026</td><td>-</td>
    </tr>
    <tr>
        <td>1</td><td>Расход</td><td>Автобус</td><td>46</td><td>03.04.2026</td><td>-</td>
    </tr>
</table>
</body>
</html>
```